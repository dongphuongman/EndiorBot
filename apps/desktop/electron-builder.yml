# EndiorBot Desktop - Electron Builder Configuration
# https://www.electron.build/configuration/configuration

appId: com.endiorbot.app
productName: EndiorBot
copyright: Copyright © 2026 EndiorBot Contributors

directories:
  output: release
  # buildResources: resources  # Disabled until icons are created

files:
  - dist/**/*
  - dist-electron/**/*
  - "!**/*.map"
  - "!**/node_modules/*/{CHANGELOG.md,README.md,README,readme.md,readme}"
  - "!**/node_modules/*/{test,__tests__,tests,powered-test,example,examples}"
  - "!**/node_modules/.bin"

# Icons will be added in design task
# extraResources:
#   - from: resources/icons
#     to: icons

asar: true
compression: maximum

# macOS Configuration
mac:
  category: public.app-category.developer-tools
  target:
    - target: dmg
      arch:
        - arm64
    - target: zip
      arch:
        - arm64
  # icon: resources/icons/icon.icns  # Will be added in design task
  darkModeSupport: true
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: entitlements.mac.plist
  entitlementsInherit: entitlements.mac.plist
  artifactName: "${productName}-${version}-${os}-${arch}.${ext}"

dmg:
  contents:
    - x: 130
      y: 220
    - x: 410
      y: 220
      type: link
      path: /Applications

# Windows Configuration
win:
  target:
    - target: nsis
      arch:
        - x64
  # icon: resources/icons/icon.ico  # Will be added in design task
  artifactName: "${productName}-${version}-${os}-${arch}.${ext}"

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: EndiorBot

# Linux Configuration
linux:
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64
  # icon: resources/icons  # Will be added in design task
  category: Development
  artifactName: "${productName}-${version}-${os}-${arch}.${ext}"

deb:
  depends:
    - libgtk-3-0
    - libnotify4
    - libnss3
    - libxss1
    - libxtst6
    - xdg-utils
    - libatspi2.0-0
    - libuuid1

appImage:
  license: LICENSE

# Publish Configuration
publish:
  provider: github
  owner: Minh-Tam-Solution
  repo: EndiorBot
  releaseType: release

# Auto-update
electronUpdaterCompatibility: ">= 2.16"
